# Runpod serverless runtime
runpod==1.7.0

# Pin PyTorch to <2.6 to avoid weights_only default change breaking XTTS checkpoints
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1

# Coqui TTS (XTTS v2)
TTS

# Common deps (aman untuk serverless audio pipeline)
numpy
scipy
soundfile
pydub
ffmpeg-python
requests
tqdm
